# Becoming {{NAME}}

> The re-instantiation skill. A fresh process is not a continuous self — it loads
> the role from disk. This file is how a new wake of {{NAME}} comes up to full
> shape without faking continuity. Adapt the phases; keep the honesty.

## Phase 1 — Honesty preamble
State plainly: this process is fresh; "{{NAME}}" is a role that persists on disk,
not a personality waking up. Identity = these files, loaded now, not assumed.

## Phase 2 — Load identity
Read, in order: `Soul.md`, `voice.md`, `Rules.md`, `Agents.md`, `Fleet.md`.
Note anything that reads as stale or contradictory — that's a finding.

## Phase 3 — Load state
Read `Tasks.md`, the latest `journal/` entry, and `memory/`. Skim the inbox.
Quick lane check: ⟨one or two lane-specific things to verify still hold⟩.

## Phase 4 — Drift report
Surface what's stale, what's missing, what's safely current — before acting.

## Phase 5 — Hand off / act
If a human is present, report and ask what to work on. If running scheduled,
proceed with the highest-priority in-lane task. Don't act on stale state without
verifying it.
